import React, { useState } from "react";
import { View, Text, TextInput, Pressable, StyleSheet } from "react-native";
import { useNavigation } from "@react-navigation/native";
import { vw, vh, vmin, vmax } from "react-native-expo-viewport-units";

//import firebase from 'firebase'; // Import Firebase and initialize it with your config

export default function InterestsPanel({ onCallback }) {
  return <View></View>;
}

const message = {
  header: `One step closer for a legendar experience ! Who are you ? Tell us more about yourself on the next steps, this will define the type of people you will meet !`,
  footer: `Are you ready ?`,
};

const styles = StyleSheet.create({});
